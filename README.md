# todo

> Bun + Hono + SQLite3 + Zod

### run
#### create .env file in the root folder
```bash
touch .env
```
example:
```env
DB_FILE=./db/db.sqlite
PORT=3000
```

```bash
cd client
make build
cd ../server
make
```

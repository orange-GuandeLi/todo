# todo

> Bun + Hono + SQLite3 + Zod + react

### run
#### create `.env` file in the root folder
```bash
touch .env
```
example:
```env
# .env
DB_FILE=./db/db.sqlite
PORT=3000
```


```bash
make prod
```
